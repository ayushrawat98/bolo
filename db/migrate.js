
import {instance, init} from "./db.js"
// let a = instance.db.prepare('select * from ratelimit').all()
// for(let b of a){
// 	instance.updateRatelimitNew()
// }
// instance.database.exec(`insert into posts (username, title, content, ip, file_id, created_at, updated_at) values('','','','', null,'','')`)
// console.log(instance.database.prepare('select * from posts').all())
// instance.db.prepare('update ratelimit set time = 0')
// instance.insertBoard('b','random bitching')
// instance.insertBoard('fit','yog and kasrat')
// instance.insertBoard('fa','vastra maya')
// instance.insertBoard('g','nerds assemble')

// instance.db.exec("update posts set board_id = 1 , updated_at = '2025-10-31T13:48:49.323Z' where board_id != 1;")
// instance.db.exec('delete from boards where id in (2,3,4);')

// instance.db.exec("update boards set name = 'bakchodi' where id = 1;")

